export const CNZ_URL = "/";

export const heroUrls = {
  base: CNZ_URL,
  routes: {
    explore: "/getstarted",
    wallet: "/wallets",
    buy: "/buycrypto",
    contact: "mailto:contact@cryptocurrency.nz",
  },
} as const;
